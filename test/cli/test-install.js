/*
 * Specification: install command
 */

// Module dependencies
var expect = require('chai').expect;

// Test suite
describe('cli:install', function() {
  
  it('should be called with a package', function() {
    
  });

});