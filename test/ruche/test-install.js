/*
 * Specification: install function
 */

// Module dependencies
var expect = require('chai').expect;
var ruche = require('../../lib/ruche');

// Test suite
describe('ruche:install', function() {
  
  it('should read the package list', function() {
    
  });
  
  it('should create a package directory in %RUCHE_TOOLS%', function() {
    
  });

  it('should create a shortcut in %PATH%', function() {
    
  });

});