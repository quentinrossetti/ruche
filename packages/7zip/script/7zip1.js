// Module dependencies.
var debug = require('debug')('ruche:packages:7zip:7zip1');

/**
 * A extractor and path recorder
 * @param {Object} match The package math in ruche.json
 * @fires RucheEmitter#install-7zip-extract
 * @fires RucheEmitter#install-7zip-path
 * @fires RucheEmitter#install-7zip-done
 */
var plainzip = function (match) {
  
};

module.exports = plainzip;