// Module dependencies.
var debug = require('debug')('ruche:packages:curl:plainzip');

/**
 * A simple extractor and path recorder
 * @param {Object} match The package math in ruche.json
 * @fires RucheEmitter#install-curl-extract
 * @fires RucheEmitter#install-curl-path
 * @fires RucheEmitter#install-curl-done
 */
var plainzip = function (match) {
  
};

module.exports = plainzip;